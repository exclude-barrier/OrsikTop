name: Apply CPU topology layer

on:
  push:
    branches: [feat/cpu-topology]
    paths:
      - .github/workflows/apply-cpu-topology.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/cpu-topology
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Apply patch
        run: python .github/scripts/apply_cpu_topology.py
      - name: Format
        run: cargo fmt --all
      - name: Validate
        run: |
          cargo check --all-targets --locked
          cargo clippy --all-targets --locked -- -D warnings
          cargo test --all-targets --locked
          cargo run --locked -- --help
      - name: Remove temporary patch files
        run: rm .github/scripts/apply_cpu_topology.py .github/workflows/apply-cpu-topology.yml
      - name: Commit result
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Generalize CPU topology detection"
          git push origin HEAD:feat/cpu-topology
