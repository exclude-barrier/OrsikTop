name: Apply smoothed LLM LAST

on:
  push:
    branches: [feature/smooth-llm-last]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/smooth-llm-last
      - uses: dtolnay/rust-toolchain@stable
      - name: Apply patch
        run: python .github/scripts/smooth_llm_last.py
      - name: Format
        run: cargo fmt
      - name: Test
        run: cargo test
      - name: Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings
      - name: Commit validated change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/ui.rs
          git commit -m "Smooth LLM sample timing display"
          git push
